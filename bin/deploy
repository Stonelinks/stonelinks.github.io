#!/bin/bash
set -e

start_time=$(date +%s)
trap 'echo "Total time: $(($(date +%s)-$start_time)) seconds"' EXIT

cd $PROJECT_ROOT

source env.sh

format
bun run build

bun run --bun http-server -p 3000 out
